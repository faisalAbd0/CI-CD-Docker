<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Show Results</title>
    </head>
    <body>
        <h1>Student Statistics</h1>

        <% if (stats && stats.length > 0) { %>
        <% const stat = stats[0]; %>
        <ul>
            <li><strong>Max Grade:</strong> <%= stat.maxGrade %></li>
            <li><strong>Min Grade:</strong> <%= stat.minGrade %></li>
            <li><strong>Average Grade:</strong> <%= stat.avgGrade %></li>
            <li><strong>Number of Students:</strong> <%= stat.studentCount
                %></li>
        </ul>
        <% } else { %>
        <p>No statistics found.</p>
        <% } %>
    </body>
</html>